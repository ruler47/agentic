use_default_settings: true

search:
  formats:
    - html
    - json

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "agentic-local-development-secret"
  limiter: false
  public_instance: false
  method: "GET"
